@model AppFluxoDeCaixa.Models.Clientes

@{
    ViewData["Title"] = "Deletar Cliente";
}

<h1 class="display-5 text-light text-center">Deletar cadasrtro de Cliente</h1>

<h3 class="mt-3 mb-3">Deseja excluir esse cadastro?</h3>
<div>
    <hr />
    <dl class="row">
        <dt class="col-3">
            @Model.ClientesNome)
        </dt>
        <dd class="col-9">
            @Model.ClientesNome)
        </dd>
        <dt class="col-3">
            @Model.Email)
        </dt>
        <dd class="col-9">
            @Model.Email)
        </dd>
        <dt class="col-3">
            @Model.Celular)
        </dt>
        <dd class="col-9">
            @Model.Celular)
        </dd>
        <dt class="col-3">
            @Model.CpfCnpj)
        </dt>
        <dd class="col-9">
            @Model.CpfCnpj)
        </dd>
        <dt class="col-3">
            @Model.DataNascFundacao)
        </dt>
        <dd class="col-9">
            @Model.DataNascFundacao)
        </dd>

    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="ClientesId" />
        <input type="submit" value="Delete" class="btn btn-danger" />
        <a class="btn btn-outline-secondary" asp-action="Index">Cancelar</a>
    </form>
</div>